# MTimer — Native macOS Time Tracker

Intuitive native macOS application for tracking time spent on projects, built with PyObjC (AppKit) and SQLite. Features start/stop/resume tracking, project management with hourly rates, period filters (Today/Week/Month), multi-language support (English/Russian/Ukrainian), and automatic dark mode.

## Requirements

- macOS 12+ (Monterey or later)
- Python 3.11–3.12 (3.12 recommended)

## Установка зависимостей (из исходников)

```bash
pip3 install -r requirements.txt
```

## Запуск из исходников

```bash
python3 mac_app.py
```

## Сборка .app (py2app)

Создаёт полноценный пакет `dist/MacikTimer.app`:

```bash
python3 setup.py py2app
```

Иконка автоматически подхватится из `assets/app_icon.icns`, если файл существует. Если его нет, можно сгенерировать из PNG:

```bash
python3 generate_icns.py  # создаст assets/app_icon.icns
```

Примечания:
- На этапе сборки бинарники автоматически подписываются ad-hoc подписью. Для распространения за пределами вашей машины рекомендуется подписать пакеты Developer ID и пройти нотарификацию Apple.
- При первом запуске из Finder может потребоваться подтверждение из Gatekeeper (ПКМ → Открыть).

## Где хранятся данные

В сборке `.app` база хранится в `~/Library/Application Support/MacikTimer/timetracker.db`.
При запуске из исходников — рядом с исходниками (`timetracker.db` в корне проекта).

## Возможности

- ⏱ Таймер: запуск/остановка, авто-разделение в полночь
- 📁 Проекты: создание, выбор и фильтрация
- � Почасовая ставка для проекта и подсчёт стоимости периода
- 🔍 Фильтры: Сегодня / Неделя / Месяц
- 🌓 Тёмная тема с мгновенным переключением
- ♻️ Восстановление активной сессии после перезапуска
- 🪟 Адаптивный интерфейс и минимальный размер окна

## Структура

- `mac_app.py` — нативный интерфейс macOS (AppKit)
- `database.py` — работа с SQLite (проекты, сессии, агрегаты по периодам)
- `setup.py` — сборка .app (py2app)
- `assets/` — иконки и ресурсы (включая `app_icon.icns`)
- `generate_icns.py` — генерация .icns из PNG
- `update_project_rates.py` — массовое обновление почасовых ставок (опционально)

## Как пользоваться

1) Запустите приложение и создайте проект (+ рядом со списком).
2) Введите описание (необязательно) и нажмите Старт.
3) Останавливайте/продолжайте сессию по кнопке.
4) Переключайте фильтры периода и/или проект — список и итоги обновятся.

## Известные заметки

- Приложение универсальное (x86_64 + arm64). Для дистрибуции за пределами разработки подготовьте подпись/нотаризацию.
- Если вы меняете тему системы, приложение обновит цвета немедленно.
