# Модуль статистики MTimer

## Опис

Модуль візуалізації даних відстеження часу з красивими інтерактивними графіками. Відкривається в окремому вікні з повним дашбордом статистики.

## Функціонал

### 6 типів графіків:

1. **Щоденна активність** — лінійний графік з area fill, показує тренд роботи по днях
2. **Розподіл по проєктах** — кругова діаграма (pie chart) топ-5 проєктів + інші
3. **Активність по годинах** — стовпчикова діаграма активності протягом дня
4. **Порівняння тижнів** — групова стовпчикова для порівняння поточного та минулого тижня
5. **Накопичувальна статистика** — area chart з кумулятивним часом
6. **Топ проєктів по прибутку** — горизонтальна стовпчикова з вартістю

## Використання

### З додатку

1. Відкрийте MTimer
2. Меню → Статистика (або Cmd+Shift+S)
3. Чекайте 2-3 секунди на завантаження даних
4. Насолоджуйтесь графіками! 📊

### З коду

```python
from statistics import StatisticsGenerator
from database import Database

db = Database()
stats = StatisticsGenerator(db)

# Показати дашборд за останні 30 днів
stats.show_statistics(period_days=30)

# Експортувати в PNG
stats.export_statistics('my_stats.png', period_days=7)
```

## Технічні деталі

**Залежності:**
- matplotlib >= 3.8.0 (візуалізація)
- numpy >= 1.26.0 (обробка даних)

**Backend:** MacOSX (нативний для macOS)

**Стиль:** seaborn-v0_8-darkgrid з кастомною палітрою

**Колірна схема:**
- Синій (#2E86AB) — основний
- Фіолетовий (#A23B72) — акцент
- Помаранчевий (#F18F01) — додатковий
- Червоний (#C73E1D) — попередження
- Зелений (#6A994E) — позитивний

## Методи класу StatisticsGenerator

### Отримання даних

```python
# Статистика по днях
daily_stats = stats.get_daily_stats(days=30)
# → {date: {'duration': seconds, 'cost': float, 'sessions': int}}

# Розподіл по проєктах
project_stats = stats.get_project_distribution(days=30)
# → {project_name: {'duration': seconds, 'cost': float}}

# Активність по годинах
hourly_stats = stats.get_hourly_distribution(days=30)
# → {hour: total_hours}

# Порівняння тижнів
current, previous = stats.get_weekly_comparison()
# → ({day: hours}, {day: hours})
```

### Візуалізація

```python
# Створити повний дашборд
fig = stats.create_dashboard(period_days=30)

# Показати вікно
stats.show_statistics(period_days=30)

# Експортувати в файл
stats.export_statistics('stats.png', period_days=7)
```

## Особливості реалізації

### Багатопотоковість

Статистика запускається в окремому потоці, щоб не блокувати головний UI:

```python
import threading

def show_stats():
    stats = StatisticsGenerator(db)
    stats.show_statistics()

thread = threading.Thread(target=show_stats)
thread.daemon = True
thread.start()
```

### Обробка помилок

- Якщо немає даних — показується повідомлення "Немає даних"
- Якщо matplotlib не встановлено — alert з інструкцією
- Усі помилки логуються через NSLog

### Форматування

- Дати: день.місяць (наприклад, 17.11)
- Час: години та хвилини (наприклад, 5г 30хв)
- Валюта: ₴ для гривні (автоматично з проєкту)

## Налаштування

### Зміна періоду

По замовчуванню показує 30 днів. Можна змінити:

```python
stats.show_statistics(period_days=7)   # Тиждень
stats.show_statistics(period_days=90)  # Квартал
```

### Експорт з високою роздільністю

```python
fig = stats.create_dashboard(period_days=30)
fig.savefig('stats_hd.png', dpi=600, bbox_inches='tight')
```

## Приклади графіків

### Інтерпретація

**Щоденна активність:**
- Пік = найпродуктивніший день
- Спад = вихідні/відпустка

**Активність по годинах:**
- 9-18 = робочі години (синій)
- Інший час = позаробочий час (фіолетовий)

**Порівняння тижнів:**
- Вищі стовпчики поточного тижня = прогрес ✅
- Нижчі стовпчики = треба підтягнутися 💪

## Troubleshooting

**Проблема:** "Модуль статистики не знайдено"
```bash
pip install matplotlib numpy
```

**Проблема:** Вікно не відкривається
- Перевірте, що matplotlib backend = MacOSX
- Спробуйте експорт замість show: `stats.export_statistics('test.png')`

**Проблема:** Графіки порожні
- Переконайтесь, що є сесії в базі даних
- Спробуйте більший період: `period_days=90`

## Майбутні покращення

- [ ] Інтерактивні графіки (hover tooltips)
- [ ] Експорт в PDF
- [ ] Фільтрація по проєктах
- [ ] Прогнозування трендів
- [ ] Порівняння періодів
- [ ] Експорт даних у CSV

---

**Автор:** Maciborka Vitalik  
**Версія:** 1.0.0  
**Ліцензія:** MIT
